# {{ meta.name }}

{{ meta.description }}

## 编程语言
{{ meta.languages | join(", ") }}

## 框架
{% if meta.frameworks %}{{ meta.frameworks | join(", ") }}{% endif %}

## 项目结构
```
{{ _render_tree(meta.structure, "") }}
```

## 依赖
{% for dep in meta.dependencies.prod %}- {{ dep }}
{% endfor %}

## 脚本命令
{% for name, cmd in meta.scripts.items() %}- `{{ cmd }}` ({{ name }})
{% endfor %}
